# AI 概念名词详解（口述串讲版）

这份稿子更偏“讲出来”的风格：我会按时间线把概念一个个串起来，你会看到它们基本都是为了解决同一件事——**怎么让大模型更像一个能干活的同事**。

你可以把它当成 10~15 分钟的分享讲稿：每一段都尽量带过渡句，讲的时候顺着读就比较连贯。

## 开场（30 秒）

最近 AI 相关的名词特别多，LLM、Prompt、RAG、Agent、MCP……看起来像一套“玄学词典”。但我自己的体感是：这些词大多数不是为了显得高级，而是工程上一步一步把坑填出来的“阶段性产物”。

所以今天我不打算逐条背定义，而是用“**为什么会出现**”把它们串起来。你只要记住一条主线：

> 先有模型能生成文字，然后我们想让它能对话；再然后想让它记得住；再然后想让它会查资料、会用工具；工具越来越多就要标准化；最后就分化出各种框架和产品形态。

## 1）大语言模型（LLM）：故事从“续写”开始

先从根上说：LLM 本质就是语言模型的“大号版本”。随着参数变大、数据变多，它在很多任务上表现会突然好一大截（大家常说的“涌现”）。

但注意啊：**LLM 最底层做的事情很朴素——根据上下文继续往下写。**

那问题来了：如果它只是“续写”，为什么我们感觉它在“聊天”？

答案是：我们给它套了一个常用的交互壳子——把输入输出分成“用户/助手”两种角色，让它每次以“助手”的口吻继续写。于是，看起来就像在对话。

到这里为止，模型有了，但我们怎么跟它说话？这就引出了下一个词。

## 2）Prompt：给模型下达任务的那段话

当你开始用 LLM，你一定会写一段输入：告诉它你要什么、给它一些背景、要求它按什么格式输出。

这段输入，后来就被大家统一叫成了 Prompt（提示词）。

你可以把 Prompt 当成“**你对模型说的完整一段话**”。

但是很快你会发现：Prompt 里其实有两类东西，一类是背景材料，一类是最终指令。于是第三个词就自然出来了。

## 3）Context：Prompt 里的“背景区”

当 Prompt 变长以后，大家会把它拆开：

- 一部分是背景、材料、历史对话、约束条件——这部分叫 Context（上下文）
- 另一部分是你这一轮“要它做什么”的指令

你会发现很多工程优化，最后都绕回这句话：**上下文给得越对，输出越靠谱。**

那接下来就遇到现实问题了：对话怎么做多轮？模型怎么“记得住”我们之前聊过什么？

## 4）Memory：让多轮对话“看起来记得住”

早期模型的一大痛点是：你问完一句，它答完就结束了；下一轮它并不会真的“记得”上一轮。

于是大家想了一个很实用的工程办法：

1. 每次新一轮提问前，把历史对话（或其中关键部分）塞回 Context
2. 再把新问题接上

从模型角度看，它只是看到了更长的上下文；但从用户角度看，它就像“有记忆”了。

这套历史信息/摘要信息的管理方式，大家就叫 Memory。

而且 Memory 还会带出一个很现实的优化：历史越来越长放不下怎么办？那就**先总结再存**，把长对话压缩成要点，下次只放要点。

到这里，我们已经把“聊天”这件事做得比较像样了。可你马上会发现第二个大问题：模型不太会“查新资料”、也不会“动手干活”。

## 5）Agent：让模型能查、能做、能闭环

模型本身通常不能直接上网搜索、不能读你本地数据库、也不能打开某个系统去点按钮。

所以大家做了一件事：写一个程序，作为“代理人”，替你去：

- 调用搜索
- 读取文件/数据库
- 调用各种工具
- 再把结果整理回 Context，让模型基于这些材料继续生成

这个“会编排流程、会调用工具”的程序，就被称为 Agent（智能体）。

这里我想强调一个口头分享很有用的理解：

> Agent 不等于“模型更聪明了”，Agent 更像是“给模型配了手脚和流程”。

那既然 Agent 能搜网络内容，自然就有人问：能不能搜“我自己的资料库/文档库”呢？这就进入 RAG。

## 6）RAG：先检索，再生成

RAG 的思路非常直白：

1. 先从外部知识源（网页、本地文档、数据库）里检索相关片段
2. 把这些片段塞进 Context
3. 再让模型生成

关键点在于：很多时候你不是缺“模型能力”，你是缺“正确材料”。RAG 的价值就是把材料喂到位。

而为了让“检索”更贴近语义而不只是关键词，常见做法是用向量检索：把文本变成向量，再找语义相近的片段。

到这里又会冒出一个工程问题：Agent 想让模型去“调用工具”，如果全用自然语言说，程序怎么稳定解析？于是就出现了 Function Calling。

## 7）Function Calling：让模型用结构化方式说“我要调用工具”

如果模型说：“你帮我查一下天气，然后把结果整理成表格。”

这句话人看得懂，但程序要稳定解析就不太舒服：到底调用哪个工具？参数是什么？

所以大家更希望模型按一个“死板但可解析”的格式回答，比如 JSON：

- 我要调用哪个函数/工具
- 参数分别是什么

这类“让模型用结构化输出描述工具调用意图”的约定，就叫 Function Calling。

它本质上不是神秘技术，更像是：**前后端约定接口格式**。

好，到这里工具开始变多了。工具多了，新的问题就来了：所有工具都写在一个 Agent 里，维护会越来越难。于是 MCP 出现。

## 8）MCP：工具多了就要“协议化/标准化”

当工具越来越多，一个常见的工程选择是：把工具做成独立服务，Agent 通过某种方式去发现和调用它们。

那这时你需要一套约定，比如：

- 怎么列出工具列表（tools list）
- 怎么调用某个工具（tool call）
- 参数怎么传、返回怎么收、错误怎么表达

这种 Agent 与工具服务之间的“调用规范/协议”，就可以理解为 MCP（模型上下文协议）。

简单一句话：**MCP 更像工具侧的“接口标准”。**

到这里你会发现：我们一直在讲“怎么把模型用起来”。那当任务越来越复杂，我们还会想把流程沉淀下来、复用起来。这时就会分化出框架与编排方式，比如 LangChain、Workflow、Skill。

## 9）LangChain：把流程用代码固化下来

当一个任务需要多步：先提取信息、再检索、再总结、再生成最终格式……

你如果每次都靠临场对话来做，成本很高，也不稳定。

于是有人选择把流程写成代码，形成“链式调用”：这类框架里比较有代表性的名字就是 LangChain。

它的优点是稳定、可控、工程化；缺点也很直观：**写死了就不太灵活**，遇到意外输入时容错要靠你在代码里一点点补。

那有没有更适合非程序员、或者更容易修改的方式？这就引出 Workflow。

## 10）Workflow：把“写代码”变成“拖拽编排”

Workflow 的核心就是低代码：把常见步骤做成积木，在页面上拖一拖、连一连。

好处是门槛低、改起来直观；但当组合爆炸、版本增多时，你会发现：拖拽的维护成本也会逐渐上来。

于是又有人想：能不能把常用能力沉淀成“可复用的说明 + 脚本目录”，让 Agent 按说明去用？这就到了 Skill。

## 11）Skill：把经验沉淀成可复用的“技能包”

当输入输出格式变多的时候，你很难为每种组合都单独做一套工作流。

一个更实用的做法是：

1. 建一个目录，把常用脚本/转换能力都放进去
2. 写一个统一说明文件，把“怎么用、怎么组合、注意事项”讲清楚

很多实现会把这个说明文件命名为 [skill.md](skill.md)，然后在 Agent 里写死“每次需要就去读它”。

从效果上看，它像是“把一段长 Prompt/流程说明”换了个地方存，并且变得可管理、可复用。

最后一个概念 Sub Agent，基本是规模上来后的“上下文管理”问题。

## 12）Sub Agent：任务复杂了就分工，顺便隔离上下文

当你让一个 Agent 处理很复杂的任务，上下文会越滚越大。

于是大家会把独立子任务拆出去，让子代理（Sub Agent）在自己的上下文里完成：

- 主 Agent 只保留结果
- 子任务过程产生的大量上下文不一定回流

你可以把它理解为：**分工 + 上下文隔离**。

到这里，主线讲完了。接下来我们把几个最容易混淆的关系，再用更口语的方式捋一遍。

## 本质思考（收束用）

### 技术本质

绕了一圈你会发现，很多名词最终都在做两件事：

1. **更聪明地组织 Prompt/Context**（把材料给到位，把结构给清楚）
2. **更自动地跑流程**（少让人来回敲字，让 Agent 去做检索、工具调用、整合）

### Agent 的本质

口头表达更好懂的一句话是：

> 把确定的事情交给程序做，把需要语义判断的分流交给大模型做。

也就是：程序负责“执行”，模型负责“判断与决策”。

### 最终目标

归根结底还是一句话：**省时间、降门槛。**

## 未来趋势（展望用）

### Token 成本

越强的 Agent 往往意味着更多上下文、更频繁的调用，所以 token 成本会更高。

但如果未来 token 变便宜、或者本地部署更普及，这个约束就会弱很多。

### 便利性优先

工程实践里，大家通常会优先选“好用、好集成、好维护”的方案，性能和资源占用往往是后面再优化。

### 超级 Agent 与产品形态

未来大概率会出现“开箱即用”的超级 Agent：常用工具接入、常用技能包、默认工作流都给你配好。

很多爆款产品的核心也不一定是技术路线有多颠覆，而是把能力做成了普通人能用的形态：接社交软件、做定时任务、可视化管理技能与流程。

## 结语（10 秒）

今天这套名词，你不需要死记。

你只要拿住主线：**从 LLM 到对话，再到记忆，再到工具与检索，再到协议与编排**。

下一次再听到新词，就直接问三个问题：

1) 它想解决什么问题？2) 它依赖的前提是什么？3) 它和相近概念的边界在哪里？

> （注：文档部分内容可能由 AI 生成）