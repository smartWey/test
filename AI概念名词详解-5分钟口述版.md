# AI 概念名词详解（5 分钟口述版）

> 目标：用一条主线把常见名词串起来。你不需要背定义，只要听懂“为什么会出现”。

## 0:00 - 0:30 开场

最近 AI 名词特别多：LLM、Prompt、RAG、Agent、MCP……看起来像一堆新名词。

但我今天想讲的很简单：这些词大多数不是为了显得高级，而是工程上一步一步把坑填出来的“阶段性产物”。

这 5 分钟我们只抓一条主线：**从模型会写字 → 能聊天 → 能记住 → 能查资料/用工具 → 工具多了要标准化 → 流程要沉淀复用。**

## 0:30 - 1:20 先从 LLM 说起

第一步是 LLM（大语言模型）。它最底层做的事其实很朴素：**根据上下文继续往下写**。

那为什么看起来像在聊天？因为我们给它套了“用户/助手”的角色壳子，让它每次以助手口吻继续写，于是就像对话。

模型有了，接下来就要想：我怎么把任务说清楚？

## 1:20 - 2:10 Prompt、Context、Memory：把“对话体验”做顺

你发给模型的那段话，就叫 Prompt（提示词）。

很快你会发现 Prompt 里有两类东西：

- 一类是背景材料、约束条件、历史信息——这部分大家叫 Context（上下文）
- 另一类是这一轮你要它做什么的指令

然后多轮对话就遇到问题：模型不会真的“记住”上一轮。

所以工程上常用的做法是：**每次新问题之前，把历史对话（或摘要）塞回 Context**，让它“看起来记得住”。这套历史信息的保存与压缩机制，就叫 Memory。

到这里，“能聊、能追问”基本就成立了。但它还不会查新资料，也不会动手干活。

## 2:10 - 3:20 Agent 与 RAG：让模型能查、能用外部资料

这时就出现了 Agent（智能体）：**一个负责流程编排和调用工具的程序**。

模型负责理解与决策，Agent 负责执行：上网搜、读文件、查数据库、调用工具，然后把结果塞回 Context，再让模型继续生成。

RAG（检索增强生成）可以理解为 Agent 的一个经典套路：

1) 先检索相关资料（网页/本地文档/数据库）
2) 把检索片段放进 Context
3) 再生成答案

一句话：**不是模型突然变聪明，而是给它喂了更对的材料。**

工具开始用起来之后，又会遇到一个“程序怎么稳定接模型输出”的问题。

## 3:20 - 4:20 Function Calling 与 MCP：工具多了就要“规矩”

当模型要调用工具，如果全用自然语言说，程序不好解析。

所以有了 Function Calling：让模型用结构化格式（比如 JSON）说清楚——“我要调用哪个工具、参数是什么”。本质上它是**模型输出格式的约定**。

再往后工具越来越多，全部塞在一个 Agent 里就难维护，于是工具被拆成独立服务。

这时需要一套“怎么列工具、怎么调用、怎么返回”的统一规范，这类约定就可以理解为 MCP（模型上下文协议）。它更像是**Agent 和工具服务之间的接口标准**。

一句话区分：**Function Calling 管“模型怎么说”，MCP 管“工具怎么接”。**

## 4:20 - 4:50 LangChain / Workflow / Skill / Sub Agent：流程复用与控复杂度

当任务很复杂，你就会想把流程沉淀下来：

- LangChain：用代码把多步流程固化，稳定可控
- Workflow：用低代码/拖拽编排，门槛更低
- Skill：把一套能力沉淀成“说明 + 资产目录”（比如 skill.md），让 Agent 按说明复用

再复杂一点，上下文会越滚越大，于是有 Sub Agent：把子任务拆出去单独跑，**分工 + 上下文隔离**。

## 4:50 - 5:00 收束

最后你不需要死记这些名词。

你只要拿住主线：**LLM 会写 → Prompt/Context 说清楚 → Memory 让对话连贯 → Agent/RAG 让它会查会用资料 → Function Calling/MCP 让工具接入更标准 → 各种编排方式让流程可复用、可治理。**

下次听到新词，直接问三句话：它解决什么问题？依赖什么前提？和相近概念边界在哪里？
